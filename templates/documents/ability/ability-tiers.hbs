<fieldset class='tab' data-group='main' data-tab='details'>
    <legend>{{fields.tiers.label}}</legend>
    {{#each fields.tiers as |field|}}
        {{formField field value=(lookup @root.source.system.tiers field.name)}}
    {{/each}}
</fieldset>